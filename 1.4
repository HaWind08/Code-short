#include<iostream>
#include<math.h>
#include<iomanip>
using namespace std;

int main()
{
	float x1,x2,y1,y2;
	cout << "Nhap dau vao: "; cin >> x1 >> x2 >> y1 >> y2;
	float D,M,C;
	D = sqrt(pow(x2 - x1, 2 ) + pow(y2 - y1, 2 ));
	M = abs(x2 - x1) + abs(y2 - y1);
	C = 1 - ((x1*x2 + y1*y2) / (sqrt(x1*x1 + y1*y1)*sqrt(x2*x2 + y2*y2)));
	cout << fixed << setprecision(3) << "D = " << D << endl;
	cout << "M = " << M << endl;
	cout << fixed << setprecision(3) << "C = " << C << endl;
}
